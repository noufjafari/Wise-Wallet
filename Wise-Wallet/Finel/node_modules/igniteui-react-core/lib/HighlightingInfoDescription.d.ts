import { Description } from "./Description";
import { SeriesDescription } from "./SeriesDescription";
import { Type } from "./type";
/**
 * @hidden
 */
export declare class HighlightingInfoDescription extends Description {
    static $t: Type;
    protected get_type(): string;
    get type(): string;
    constructor();
    private s;
    get startIndex(): number;
    set startIndex(a: number);
    private r;
    get endIndex(): number;
    set endIndex(a: number);
    private v;
    get state(): string;
    set state(a: string);
    private p;
    get progress(): number;
    set progress(a: number);
    private l;
    get isMarker(): boolean;
    set isMarker(a: boolean);
    private k;
    get isFullRange(): boolean;
    set isFullRange(a: boolean);
    private j;
    get isExclusive(): boolean;
    set isExclusive(a: boolean);
    private h;
    get series(): SeriesDescription;
    set series(a: SeriesDescription);
}
